test "basic arithmetic"
    assert_eq (2 + 2) 4
    assert_lt 3 5
    assert_gt 10 3
    assert_not_eq 5 6

test "string"
    assert_eq ("hello" ++ " world") "hello world"
    assert (len "hello") == 5

test "list"
    my_list: [1,2,3]
    assert_eq (len my_list) 3
